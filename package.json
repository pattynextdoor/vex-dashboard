{
  "name": "vex-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Esoteric singularity dashboard — WebGL + chat + controls",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.172.0"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  }
}
